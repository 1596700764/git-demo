#该文件优先级高于application.yml，重复内容可删除
spring:
  application:
    name: userservice #服务名称
  profiles:
    active: dev #开发环境
  cloud:
    nacos:
      server-addr: localhost:8848 #nacos的地址#
      config:
        file-extension: yaml #后缀名